spring.application.name=Lab12PartCClient

# Logging configuration
logging.level.com.example.jwtclient=INFO
logging.level.org.springframework.web.client=DEBUG

# Server configuration (not running as web server)
spring.main.web-application-type=none